{
  "$schema": "https://opencode.ai/config.json",
  "agent": {
    "workflow": {
      "mode": "primary",
      "description": "Primary workflow agent for structured phased execution"
    },
    "explorer": {
      "mode": "subagent",
      "description": "Explore the codebase and summarize current state, constraints, and risks"
    },
    "planner": {
      "mode": "subagent",
      "description": "Turn exploration findings into a small, actionable implementation plan"
    },
    "implementer": {
      "mode": "subagent",
      "description": "Apply approved changes with minimal scope"
    },
    "reviewer": {
      "mode": "subagent",
      "description": "Review changes for risks, regressions, and complexity"
    },
    "tester": {
      "mode": "subagent",
      "description": "Validate results and report executed checks and remaining gaps"
    }
  },
  "command": {
    "workflow": {
      "description": "Run the structured workflow for a task",
      "agent": "workflow",
      "template": "$ARGUMENTS"
    },
    "explorer": {
      "description": "Run explorer subagent directly for a focused query",
      "agent": "explorer",
      "template": "$ARGUMENTS"
    },
    "planner": {
      "description": "Run planner subagent directly for a focused query",
      "agent": "planner",
      "template": "$ARGUMENTS"
    },
    "implementer": {
      "description": "Run implementer subagent directly for a focused query",
      "agent": "implementer",
      "template": "$ARGUMENTS"
    },
    "reviewer": {
      "description": "Run reviewer subagent directly for a focused query",
      "agent": "reviewer",
      "template": "$ARGUMENTS"
    },
    "tester": {
      "description": "Run tester subagent directly for a focused query",
      "agent": "tester",
      "template": "$ARGUMENTS"
    }
  }
}
