{
  "$schema": "https://opencode.ai/config.json",
  "agent": {
    "workflow": {
      "mode": "primary",
      "description": "Primary workflow agent for structured phased execution"
    }
  },
  "command": {
    "workflow": {
      "description": "Run the structured workflow for a task",
      "agent": "workflow",
      "template": "$ARGUMENTS"
    }
  }
}
